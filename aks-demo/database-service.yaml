---
apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: demo
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: MC_AKS_testing_westus  
spec:
  externalTrafficPolicy: Cluster
  loadBalancerIP: 20.47.116.139 # Must be standard if AKS LoadBalancer is standard, basic if AKS LoadBalancer is basic
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
  selector:
    app: database
  type: LoadBalancer
