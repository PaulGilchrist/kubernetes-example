---
apiVersion: v1
kind: Service
metadata:
  name: queue-service
  # namespace: default
spec:
  type: LoadBalancer # so a listner can be outside the cluster without needing an ingress
  ports:
    - port: 15672
      name: http
    - port: 5672
      name: amqp
  selector:
    app: queue
